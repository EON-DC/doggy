spring:
  datasource:
    driver-class-name: org.h2.Driver
    username: sa
    password:
    url: jdbc:h2:tcp://localhost/~/product
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    database: h2
  h2:
    console:
      enabled: true